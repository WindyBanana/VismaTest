@page
@model VismaTest.Pages.PositionMissions.IndexModel

@{
    ViewData["Title"] = "Index";
    Layout = "~/Pages/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.PositionMission[0].PositionTitle)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PositionMission[0].MissionName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.PositionMission) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.PositionTitle)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MissionName)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.PositionTaskID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.PositionTaskID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.PositionTaskID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
